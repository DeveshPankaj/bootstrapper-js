
# Dependency directories
node_modules/
.claude/
.venv/
v2/

# scratch/unrelated scripts, not part of the app
temp/.DS_Store
**/.DS_Store
__pycache__/
*.pyc

# generated test screenshots — local only, not tracked
testing/screenshots/
.githooks/

# WISP relay server for the Alpine WASM Linux VM app — a general-purpose
# outbound network relay (see relay/README.md); a local dev tool, not
# something meant to ship with the publicly-hosted app.
relay/
